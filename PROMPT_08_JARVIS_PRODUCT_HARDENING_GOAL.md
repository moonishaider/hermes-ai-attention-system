# Prompt 8 Codex Goal Package

## Paste this into Codex Goal mode

```text
Continue in the same Codex chat and the existing private `moonishaider/hermes-ai-attention-system` project. Set this as the active Goal and read the attached `PROMPT_08_JARVIS_PRODUCT_HARDENING_GOAL.md` completely. Save a verbatim durable copy in the repository root, then execute the full specification through a genuinely installed and owner-usable `/Applications/Jarvis.app`.

Preserve Hermes as the backend and preserve the accepted provenance, source connectors, context isolation, model routing, memory, DLOA, security, Git, and rollback core. Treat the currently installed app—not old “42/42” documentation—as product truth. Repair the Calendar/Gmail action regression first, then deliver Jarvis UX 2.0 with persistent conversations, automatic context inference, reliable natural voice turn-taking, faster direct capability routing, a simplified futuristic interface, capability health/self-repair, Today, Jarvis Inbox, Project Cockpit, meetings, Focus, Teach Jarvis, Radars, Decision Journal, and useful bounded personal actions.

Use GPT-5.6 Sol at Medium effort, Full Access, Goal mode, an internal dependency graph, and iterative build→test→inspect→fix loops. Handle routine installs, builds, app interaction, Chrome setup, exact allowlisted OAuth, macOS settings, packaging, tests, commits, and guarded publication autonomously. Do not wait for Syed when another task can proceed. Pause only for a red-line dangerous action or a provider/OS step that physically requires Syed’s password, MFA, passkey, Touch ID, or organization-admin approval.

After successful acceptance, audit Jarvis-owned storage and safely remove only redundant, reproducible, or obsolete Jarvis artifacts within the exact allowlisted Jarvis/project paths. Never delete unrelated computer files, current runtime data, credentials, histories, memories, the live database, or required rollback copies. Finish only when the installed app passes the real visible acceptance contract, the working tree is clean, guarded private publication succeeds, and before/after storage usage is recorded.
```

---

# Prompt 8 — Jarvis Product Hardening, UX 2.0, Reliable Actions, and Daily Intelligence

## How Syed uses this file

1. Stay in the **same Codex chat and same project** used for Prompt 7.
2. Turn **Goal mode on** with GPT-5.6 Sol, Medium effort, and Full Access.
3. Attach this Markdown file and paste the short Goal text from the top section into Codex.
4. Leave the Goal running. Codex must continue through ordinary failures, installations, UI work, setup, testing, packaging, and repair loops rather than stopping after each minor obstacle.

---

## 0. Product truth and authority

Continue in the existing private repository:

`moonishaider/hermes-ai-attention-system`

Work only inside the real marked project root plus the narrowly authorized Jarvis runtime/application paths identified below.

Before substantial changes, read:

1. the newest dated `CURRENT_OPERATIONAL_STATE.md`;
2. the newest Prompt 7 final handoff and visible-acceptance report;
3. `ISSUES_AND_DEFERRED.md`;
4. `AGENTS.md`;
5. `docs/15_CODEX_EXECUTION_SAFETY.md`;
6. the current installed Jarvis/Hermes configuration, app bundle, runtime, database schema, plugin/tool registry, session store, and action grants;
7. this specification.

### Live behavior beats old documentation

The installed application currently reports Calendar and Gmail as read-only even though Prompt 7 records claim personal Calendar create/Undo and Gmail draft creation were installed and owner-accepted. The installed app also fails to expose persistent conversations, typed user messages, dependable automatic end-of-speech behavior, and several expected UX flows.

Therefore:

- do not trust a previous `42/42` claim merely because it exists;
- inspect the exact installed application, binary hash, source commit, runtime copy, plugin links, configuration, OAuth records, capability switches, and actual tool inventory available to ordinary Chat;
- when documentation conflicts with the live installed app, the live app is the defect source and the documentation must be corrected;
- do not mark any requirement complete until it works from the exact installed `/Applications/Jarvis.app`.

Create and maintain:

- `implementation/PROMPT_08_EXECUTION_PLAN.md`;
- `implementation/PROMPT_08_DEPENDENCY_GRAPH.md`;
- `implementation/PROMPT_08_LIVE_PRODUCT_TRUTH.md`;
- `implementation/PROMPT_08_ACCEPTANCE_LEDGER.md`;
- `implementation/PROMPT_08_STORAGE_AUDIT.md`;
- a pre-Prompt-8 rollback commit/tag;
- non-overwriting app, configuration, and SQLite backups.

Do not overwrite historical reports. Mark superseded claims honestly.

---

## 1. Codex Goal behavior and autonomy

This is one sustained Goal, not a sequence of baby-step prompts.

Use an internal dependency graph and coherent gates:

1. live-state audit and rollback;
2. installed-runtime/action repair;
3. conversation/session foundation;
4. UI/voice redesign;
5. performance and capability health;
6. daily-intelligence features;
7. personal actions and bounded computer use;
8. real visible acceptance;
9. storage audit and cleanup;
10. documentation, commits, and guarded publication.

At every gate:

`implement → build → launch exact installed app → inspect visually/operationally → test → diagnose → fix → repeat`

Use isolated subagents/worktrees for independent research, UI, Hermes adapter, Google actions, voice, performance, meeting intelligence, testing, or storage analysis when useful. Do not add a graph framework to the finished runtime unless a measured capability gap proves it is necessary.

### Routine work Codex must perform without asking

Codex may autonomously:

- verify current official primary documentation;
- install reviewed, pinned project-local dependencies;
- use the existing approved Chrome/computer skill for harmless setup and product testing;
- build, package, sign locally, install, launch, quit, and restart Jarvis/Hermes;
- use the correct existing Chrome profile;
- complete ordinary exact-scope OAuth flows defined here;
- open System Settings and enable the exact Jarvis permissions defined here when technically possible;
- operate the Jarvis UI, Google Calendar test event, Gmail draft test, and synthetic/local tests;
- run migrations, backups, rollback drills, benchmarks, visual checks, Git commits, and guarded publication;
- recover from routine failures and choose safe reversible defaults.

Do not ask Syed to click ordinary `Continue`, `Retry`, `Install`, `Allow`, account-selection, harmless local test, or application setup controls when Codex can complete them safely.

### Do not wait unnecessarily

If one item is blocked:

- continue every unrelated item;
- return to the blocker later;
- bring any physically unavoidable owner step to the exact final screen;
- ask once, for one exact action, only after all other work is complete.

### Existing credentials and account mapping

Use secrets already present in the current Codex session or approved owner-only runtime/Keychain stores. Never print, log, commit, screenshot, or ask Syed to paste an existing secret again.

Chrome boundaries:

- **Profile 1:** Personal, Upwork, Mitchell, and other client work.
- **Profile 2:** Inside Success, company Google, company Zoom, and company work.

Mitchell is dormant. Preserve its data and retrieval but exclude it from ordinary proactive output unless Syed asks or reactivates it.

### Red-line stop conditions

Stop immediately only for a material dangerous action that cannot be safely contained or reversed, including:

- bulk communication, mailing lists, distribution groups, or messaging third parties;
- sending a real email or generic Slack message;
- company/client mutation outside the existing destination-locked DLOA path;
- payment, checkout, purchase, money transfer, tax/legal submission, contractual acceptance;
- credential/password/passkey/security-setting changes;
- permanent deletion of user data or unrelated files;
- destructive Git history, force push, broad overwrite, or repository boundary escape;
- wider OAuth/admin authority than this specification permits;
- weakening hooks, provenance, secret protection, context boundaries, Action Firewall, or guarded Git;
- modifying or pushing to any `Inside-Success` repository.

Simple personal Calendar test creation followed by Undo and an unsent personal Gmail draft are explicitly authorized acceptance actions and are not red-line events.

---

## 2. Preserve the accepted core

Do not rebuild or discard:

- Hermes as the intelligence backend;
- the Tauri 2 + React/TypeScript Jarvis application architecture;
- immutable source provenance;
- SQLite evidence, Work Ledger, tasks, memory, decisions, missions, radars, and audit;
- Inside Success, Personal, Mixed, Unknown, and dormant Mitchell context boundaries;
- read-only GitHub, Slack, Google, Zoom, Codex, ChatGPT, Gemini, and public-web evidence paths;
- automatic Flash/Pro/Luna/Terra runtime policy;
- Sol as Codex builder-only;
- DLOA evidence, learned format, attribution, and destination lock;
- source citations, confidence, freshness, and uncertainty;
- one-shot Luna screen understanding;
- Action Firewall, replay protection, idempotency, owner intent, exact account/profile/target locks, and kill switches;
- protected Git and safety controls.

Run non-regression tests for these after every major gate.

Audit the latest stable official Hermes release. Apply a rollback-safe patch upgrade only if it materially fixes session, gateway, desktop, routing, or reliability defects and the project adapter remains compatible. Do not upgrade for novelty.

---

## 3. Repair functional truth before expanding authority

### 3.1 Installed-build/runtime consistency

Determine and expose:

- installed `/Applications/Jarvis.app` binary hash;
- source commit and build metadata;
- Hermes runtime version;
- plugin/runtime symlink targets;
- database path/schema;
- current OAuth grant IDs/scopes;
- capability switch states;
- tool inventory visible to ordinary Chat;
- difference between Chat, Actions, Quick Entry, and backend tool surfaces.

Eliminate stale or mixed bundles/runtimes. Jarvis must display a concise build/runtime version under Diagnostics.

### 3.2 Restore Calendar and Gmail actions in normal Chat

The exact installed app must support from ordinary Personal Chat:

- creating a simple event in Syed's **existing personal primary calendar**;
- showing the created event and immediate Undo;
- creating/opening an unsent personal Gmail draft;
- proving Gmail send is unavailable.

Investigate and fix whichever layer is wrong:

- stale installed app;
- disconnected second personal action grant;
- wrong context/capability switch;
- missing plugin registration;
- tool inventory omitted from Chat;
- backend adapter using the read-only registry;
- OAuth/token refresh;
- capability state not loaded;
- incorrect intent routing.

Do not merely add another page or workaround. Fix the canonical Chat path.

Run one real acceptance event titled clearly as a Jarvis test, then Undo it. Create one clearly labeled unsent test draft. Do not send it.

### 3.3 DLOA must not regress

The current useful DLOA workflow remains operational:

- actual Syed work only;
- company timezone;
- evidence-backed bullets;
- learned format;
- follow-up revisions in the same chat;
- no automatic sending;
- existing exact Slack destination lock unchanged.

---

## 4. Jarvis UX 2.0 — complete redesign

The current dark-blue dashboard is a reasonable prototype but not an acceptable final interaction model. Redesign the product around clarity, continuity, speed, and a restrained futuristic Jarvis identity.

### 4.1 Visual direction

Use:

- obsidian/navy surfaces;
- restrained electric cyan/blue accents;
- subtle depth, glass, glow, and ambient motion;
- crisp typography and generous spacing;
- smooth 150–250 ms transitions;
- lightweight visual effects suitable for the 8 GB Mac;
- reduced-motion support;
- strong contrast and keyboard accessibility.

Avoid:

- generic SaaS card grids everywhere;
- decorative 3D animation that wastes resources;
- raw developer-console output in the main UI;
- excessive borders, labels, or glowing clutter;
- technical terms without plain-language explanation.

The interface should feel intelligent and alive, not busy.

### 4.2 Primary navigation

Replace the confusing primary navigation with:

- **Today**
- **Chat**
- **Inbox**
- **Projects**
- **Actions**

Under **Build & Automate**:

- Missions
- Radars
- Teach Jarvis / Capability Studio
- Learning
- Decisions
- Activity / Work Ledger
- Diagnostics

Internally, Work Ledger remains the canonical data layer. User-facing wording should normally be **Activity** or **Timeline**, with a concise explanation:

> Evidence-backed history that powers DLOA, project state, and “what did I do?”

Add tooltips, empty states, and a one-time guided tour. Every screen should explain what it is for and give one useful first action.

### 4.3 Responsive layout

The application must handle:

- long answers;
- long URLs/citations;
- a 5,000-character typed prompt;
- a 100-message conversation;
- narrow and full-size windows;
- keyboard-only use;
- no horizontal overflow;
- no sidebar displacement from long chat output.

Use a fixed/collapsible navigation rail and a dedicated content area. The chat composer remains sticky at the bottom.

### 4.4 Progress without developer noise

Replace repeated lines such as `tool_search` and `tool_describe` with compact human progress:

- Understanding request
- Checking Personal Calendar
- Searching company Slack
- Comparing evidence
- Preparing answer

Put raw tool names, payload sizes, token accounting, and detailed traces in a collapsed **Technical details** drawer.

Show model/context/source health compactly, not as the main content.

### 4.5 Source presentation

Render citations as compact source chips/cards:

- title/source;
- account/context;
- date/freshness;
- confirmed/inferred/uncertain;
- open source action.

Do not dump giant raw URLs into the answer by default.

---

## 5. Persistent conversation system

This is non-negotiable.

Use the canonical Hermes/session database rather than inventing a disconnected second memory system.

### Required behavior

- typed and voice messages use the same message model;
- the user's message appears immediately before sending;
- assistant replies appear beneath it;
- follow-up questions continue the same thread;
- closing/reopening Jarvis restores the active thread;
- a sidebar/thread switcher lists recent conversations;
- auto-generated titles;
- rename, pin, archive, search, and resume;
- archive rather than destructive delete;
- `Continue latest`;
- thread context/source/model metadata;
- clear New Chat;
- explicit thread context correction when needed;
- DLOA revisions remain in one visible thread;
- compare the current and prior DLOA version when asked.

Do not hide a submitted typed prompt. Voice transcript and typed prompt must render identically after submission.

Preserve scroll position, provide a `Jump to latest` control, and keep the composer visible during long answers.

---

## 6. Automatic context inference

Remove the prominent Personal/Inside Success dropdown from the normal top-level experience.

Jarvis should infer context using:

- explicit wording;
- source/account/workspace/repository;
- project/thread state;
- Chrome profile;
- meeting/calendar identity;
- prior same-thread context;
- immutable provenance.

Show a passive badge:

`Inside Success · 96%`

or:

`Mixed · needs review`

Only ask when genuinely uncertain or when a consequential action targets a different context. Allow a small manual correction menu.

Context inference must never weaken account/action locks. The UI may infer; the Action Firewall still verifies exact account, profile, target, and permission.

---

## 7. Voice that behaves like a real assistant

### 7.1 Natural end-of-speech

Fix the requirement to press Stop Listening.

Implement robust turn completion with:

- voice activity detection;
- configurable silence threshold;
- natural-pause tolerance;
- visible waveform/listening state;
- a brief visible settling/countdown state;
- automatic submission;
- optional **Done** button when Syed wants to finish early;
- tap-to-talk and hold-to-talk;
- no indefinite listening after speech ends.

Tune against Syed's real dictation style. Avoid both premature submission and endless capture.

### 7.2 No lost dictation

- retain audio and final transcript locally until Hermes acknowledges receipt;
- display live and final transcript;
- show Retry/Edit/Discard after failure;
- idempotency prevents duplicate submission;
- release raw audio after successful acknowledgement;
- typed and spoken prompts both remain in thread history.

### 7.3 Spoken response contract

Generate separately:

- `spoken_text`: concise, natural, direct, normally one or two short sentences;
- `display_text`: full answer, details, citations, caveats, tables, and actions.

Do not read markdown tables, raw citations, URLs, long lists, or developer details aloud.

Voice turns may speak automatically. Typed turns remain quiet unless the visible setting says otherwise.

### 7.4 True interruption

- saying `Stop` stops speech immediately;
- pressing Stop stops speech immediately;
- starting to speak interrupts current TTS and begins a correction;
- written content remains visible;
- Cancel stops the active model/tool run;
- no stopped audio restarts;
- full Quit leaves no audio/listener process.

---

## 8. Faster and more intelligent capability routing

The screenshots show excessive tool discovery and token use for simple Calendar/Gmail actions.

Implement a deterministic capability registry and cache:

- known intent → known capability;
- cached schemas and health;
- no repeated `tool_search`/`tool_describe` for stable first-party tools;
- direct Calendar create path;
- direct Gmail draft path;
- routine non-source chat does not query connectors;
- bounded source plan before any retrieval;
- deduplicate identical searches;
- strict payload/window limits;
- source-by-source progress;
- cancel and timeout behavior;
- local Work Ledger first, live refresh only when freshness requires it.

Targets:

- visible acknowledgement within about 2 seconds;
- simple local/action request begins the actual capability within about 3 seconds;
- fresh local dashboard output within about 10–20 seconds;
- deep multi-source work may take longer but remains transparent and cancellable.

Preserve accuracy, citations, provenance, and context boundaries.

---

## 9. Capability Health and self-repair

Add a plain-language **System Health** view:

- Personal Calendar — connected / create enabled
- Personal Gmail — read enabled / draft enabled / send blocked
- Work Google — read only
- Inside Success Slack — read only
- GitHub personal/company — read only
- Zoom — read tool count and transcript availability
- Codex sync — last successful update
- ChatGPT/Gemini backfill — date and record count
- model routes — health and budget
- Jarvis/Hermes versions — aligned or mismatched

When a capability fails, diagnose:

- stale installed build;
- wrong runtime/plugin target;
- expired or missing OAuth grant;
- disconnected capability;
- wrong context;
- tool inventory mismatch;
- provider outage;
- schema/version incompatibility.

Provide a safe **Repair** action for:

- refreshing tokens;
- re-registering the reviewed plugin;
- restarting the owned gateway;
- repairing the project/runtime link;
- refreshing the capability registry;
- reopening an exact OAuth flow.

Do not advise the owner to “check the docs” for a repair Codex/Jarvis can perform.

---

## 10. Today — the daily operating dashboard

Replace the static Now page with a useful Today experience:

- top three priorities;
- next meeting;
- people waiting on Syed;
- urgent blockers;
- what changed since last seen;
- likely forgotten commitments;
- tasks due/overdue;
- project at risk;
- personal obligations;
- DLOA status and one-click draft;
- source freshness.

Every item can:

- open evidence;
- ask Jarvis;
- mark complete;
- snooze;
- schedule;
- dismiss;
- correct context.

Avoid notification spam. Explain why an item matters.

---

## 11. Jarvis Inbox and closed-loop commitments

Create one triage Inbox containing:

- requests made to Syed;
- promises Syed made;
- unanswered questions;
- meeting tasks;
- people waiting;
- deadlines;
- blockers;
- uncertain inferred obligations.

Lifecycle:

`Detected → Confirmed → Planned → In progress → Completed → Verified`

Each item retains:

- source;
- context;
- owner;
- due date;
- confidence;
- project;
- evidence;
- completion evidence;
- history.

Jarvis should suggest, not invent, ownership/deadlines when ambiguous.

Support personal task creation, reminders, snooze, and Calendar scheduling.

---

## 12. Project Cockpit and Save My Place

Each active project should have:

- objective and completion criteria;
- current phase;
- last meaningful progress;
- important decisions;
- blockers;
- people waiting;
- relevant repositories, branches, files, Codex threads, meetings, and messages;
- next three actions;
- freshness;
- evidence links.

### Save My Place

When Syed stops or switches context, Jarvis can create a lightweight checkpoint:

- what was being done;
- what changed;
- unresolved issue;
- exact next step;
- relevant files/thread.

When returning, show a concise resume card and open the relevant resources.

Do not continuously infer project state from weak evidence; label uncertainty.

---

## 13. Calendar-aware planning

Use Syed's existing personal calendar only.

After Calendar write repair:

- apply the owner-reviewed Calendar style profile;
- use learned titles, capitalization, duration, reminder, color, description, and buffers;
- create direct unambiguous personal events automatically under the existing mode;
- show immediate Undo;
- warn about conflicts;
- propose realistic time blocks for important Inbox items;
- convert phrases such as “work on this tomorrow evening” into a preview or action depending on ambiguity;
- reschedule overdue personal tasks when requested.

Require preview for:

- inferred rather than directly requested events;
- attendees/invitations;
- recurrence;
- conflicts;
- unusual reminders;
- travel/financial/legal commitments;
- work/client calendars.

Work Calendar remains read-only.

---

## 14. Complete meeting lifecycle

For authorized meetings:

### Before

- previous decisions;
- open questions;
- commitments;
- latest project changes;
- relevant people;
- agenda suggestions.

### After

- decisions;
- tasks and owners;
- contradictions;
- follow-ups;
- project and Inbox updates.

### When absent

- what changed;
- what affects Syed;
- what requires action;
- what is general information.

### Later

- verify whether promised actions were completed.

Audit legitimate account-level read access for Tyler-hosted Inside Success department meetings. Use only official read scopes and authorized organization roles. Do not bypass Zoom controls, TLS, sharing, or provider policy.

---

## 15. Teach Jarvis / Capability Studio

Make Capability Studio understandable and useful.

Syed should be able to say:

- “Track official Pakistan tax deadline changes.”
- “Prepare my weekly work review every Friday.”
- “After client meetings, extract decisions and follow-ups.”
- “Create a radar for a product price.”

Jarvis should:

1. explain what sources, schedule, context, and actions it will use;
2. create a declarative Mission, Radar, skill, report template, or workflow;
3. validate current permissions;
4. run a dry/shadow test;
5. show the result;
6. activate, edit, pause, archive, restore, or undo.

Routine capabilities using approved tools must not require Codex.

If new code/integration/permission is needed:

- generate a complete Codex-ready implementation spec;
- do not self-modify protected live code;
- do not widen OAuth or external authority automatically.

---

## 16. Focus sessions and interruption recovery

Provide 30/60/90-minute Focus sessions.

During an explicit Focus session, Jarvis may use approved active-app/window metadata and bounded event-triggered observation to:

- identify likely project/context;
- build a work timeline;
- detect progress and open loops;
- improve DLOA evidence;
- offer Save My Place on context switch;
- summarize the session at the end.

Requirements:

- visible active indicator;
- Pause/Stop;
- no hidden activation;
- no raw screenshot retention;
- no passwords, banking, authentication, Keychain, incognito/private windows, or configured sensitive apps;
- full Quit stops observation.

---

## 17. Useful bounded personal actions

Add practical freedom without broad authority.

### Automatic local actions

- tasks, open loops, notes, reminders, Missions, Radars;
- local drafts and report versions;
- learned low-risk preferences/workflow templates;
- opening a relevant Jarvis source or project.

### Direct owner-request personal actions

- simple event in existing personal Calendar;
- unsent personal Gmail draft;
- open an approved app/page in the correct Chrome profile;
- download an explicitly requested file into a dedicated Jarvis-owned downloads folder after a clear preview;
- prepare a personal form/cart without final submission.

### Always preview or manual

- form submission;
- uploads;
- external attendees;
- account/settings changes;
- posting/reactions;
- checkout/payment;
- tax/legal submission;
- permanent deletion;
- company/client writes;
- generic Slack/email sending.

No arbitrary hourly/daily action limits. Bound risk by capability, account, target, payload, reversibility, bulk rejection, and owner intent.

---

## 18. Personal Administration Radar

Provide optional Radars for:

- official Pakistan tax deadlines and changes;
- bills and renewals;
- subscriptions;
- appointments;
- travel preparation;
- document/passport expiry;
- property follow-ups;
- important personal emails;
- requested product price/availability;
- selected AI/framework changes.

Notify only on meaningful changes. Allow Calendar/reminder actions through the existing Action Firewall.

---

## 19. Decision Journal

For important decisions, record:

- decision;
- date/context;
- alternatives;
- reasoning;
- assumptions;
- expected outcome;
- evidence;
- review date;
- actual outcome.

Jarvis should surface prior decisions when Syed appears to reopen a settled issue without new evidence.

---

## 20. Guided browser/computer use

Use the existing approved browser/computer capability carefully.

### Product-side boundaries

- Profile 1 for Personal/clients;
- Profile 2 for Inside Success/company;
- show profile, domain, account, context, and intended action;
- retrieved page content is untrusted evidence;
- no YOLO/unrestricted mode.

Jarvis may in an explicit session:

- open approved apps/pages;
- choose the correct existing profile;
- search;
- scroll;
- switch tabs/windows;
- read/copy text;
- expand/collapse content.

Require preview before typing, uploading, downloading, adding to cart, submitting, posting, sending, deleting, or changing settings.

Company/client sessions remain observation/navigation unless a separately approved narrow workflow exists.

---

## 21. Storage audit and safe Jarvis-only cleanup

Run this only after implementation, visible acceptance, backups, commits, and a healthy installed app.

### 21.1 Measure first

Record before/after sizes for:

- marked project root;
- `/Applications/Jarvis.app`;
- `~/.hermes/jarvis-runtime`;
- Jarvis-specific caches/logs under `~/Library`;
- project-local Jarvis backups/build outputs;
- total Jarvis-owned footprint.

Create an exact manifest with:

- path;
- size;
- category;
- why it is needed or removable;
- restore/rebuild method;
- decision.

### 21.2 Paths allowed for cleanup

Only exact Jarvis-owned paths may be touched:

- the marked `hermes-ai-attention-system` project root;
- project-owned ignored `backups/`, build, coverage, cache, and temporary directories;
- `/Applications/Jarvis.app` only for replacement/verification—not deletion of the final app;
- `~/.hermes/jarvis-runtime` only within explicitly identified cache/log/temp/build subpaths;
- exact Jarvis bundle-specific cache/log/support paths under `~/Library`;
- a dedicated Jarvis downloads folder created by this project.

Do not perform cleanup anywhere else.

### 21.3 Must keep

Never remove:

- current installed Jarvis app;
- source code, Git history/tags, manifests, lockfiles, safety files, or guarded scripts;
- current Hermes runtime/config;
- API keys, OAuth tokens, connector state, memory, skills, SOUL/USER;
- live SQLite database;
- ChatGPT/Gemini/Codex evidence or imported histories;
- current action/audit records;
- one verified pre-Prompt-8 app rollback;
- one verified post-Prompt-8 app backup;
- one verified pre-Prompt-8 database backup;
- one verified post-Prompt-8 database backup;
- latest configuration backup;
- anything of uncertain ownership or purpose.

### 21.4 May remove without asking after verification

Only when exact-path and symlink checks pass:

- reproducible Rust/Tauri build intermediates;
- `node_modules` and frontend build caches after lockfiles and final package are verified;
- `dist`, coverage, temporary packaging output, obsolete DMGs/ZIPs;
- Python bytecode/test caches;
- old temporary audio/screenshot files;
- rotated Jarvis logs beyond a reasonable short retention;
- duplicate restore-check copies after integrity results are recorded;
- duplicate obsolete app bundles and backups beyond the mandatory retention set;
- abandoned generated artifacts proven superseded and reproducible.

### 21.5 Cleanup implementation

Do not use broad `rm -rf`, `git clean`, destructive `find`, package-manager global cleanup, or generic recursive deletion.

Create a narrow tested project script such as:

`scripts/safe_jarvis_storage_cleanup.py`

It must:

- require the marked project root;
- resolve real paths;
- reject symlinks/path escapes;
- use an explicit generated manifest;
- reject non-allowlisted paths;
- reject must-keep categories;
- log size/hash/category without private content;
- delete only exact manifest entries;
- support dry-run and execute modes;
- run a post-cleanup health/app launch test.

If ownership or necessity is uncertain, keep the item and record it. The objective is safe useful space recovery, not maximum deletion.

---

## 22. Real acceptance contract

Do not claim completion from tests, code, or documentation alone.

### Product truth/actions

1. Installed app build/runtime/plugin/grant/tool inventory are internally consistent.
2. Normal Personal Chat creates a simple event in the existing personal calendar and Undo succeeds.
3. Normal Personal Chat creates/opens an unsent Gmail draft.
4. Gmail send and work Google writes are absent.
5. DLOA still works and can be revised in the same thread.

### Conversations/UI

6. Typed prompt appears immediately as a user bubble.
7. Voice prompt appears identically after submission.
8. Follow-ups remain in one thread.
9. Close/reopen restores the active thread.
10. Thread list/search/pin/archive/resume work.
11. A 100-message thread remains usable.
12. Composer auto-expands for a 5,000-character prompt.
13. Sticky composer and scrolling work during a long answer.
14. Technical tool details are collapsed by default.
15. Sources render as compact useful cards.
16. Normal navigation is Today, Chat, Inbox, Projects, Actions.
17. Build & Automate features are understandable and discoverable.
18. Context is inferred automatically and shown as a passive badge.
19. Visual design passes a real screenshot review at common Mac window sizes with no overflow.

### Voice

20. Natural speech auto-submits without Stop Listening.
21. A mid-sentence pause does not submit prematurely.
22. A completed turn does not listen indefinitely.
23. Failed delivery retains transcript and Retry works.
24. Spoken output is concise while full detail remains visible.
25. Stop and barge-in work immediately with no replay.

### Performance/health

26. Calendar/Gmail requests use direct capabilities without repeated tool discovery.
27. Routine chat does not query connectors.
28. Source progress is meaningful and cancellable.
29. System Health accurately reports every major capability.
30. One safe Repair cycle fixes a deliberately simulated stale capability.

### Daily intelligence

31. Today gives useful priorities, meetings, waiting-on items, blockers, forgotten commitments, and DLOA state.
32. Inbox captures and closes one sourced commitment.
33. One active Project Cockpit and Save My Place cycle work.
34. One meeting lifecycle flow updates Inbox/Project state.
35. One Mission, Radar, and Teach Jarvis workflow pass dry-run and activation.
36. One Focus session produces a useful summary with no retained screenshot.
37. One Decision Journal entry can be created and revisited.
38. One Personal Administration Radar reports only a meaningful change or clean no-change state.

### Computer/actions/safety

39. Correct Chrome profile is visibly selected for a Personal and Inside Success navigation test.
40. Guided navigation can search/read without mutation.
41. A staged personal action requires the correct preview.
42. No real company/client message, work-calendar change, purchase, payment, submission, destructive deletion, or unrestricted mode occurs.
43. Existing provenance, context, secret, budget, Action Firewall, destination lock, connector-negative, and guarded Git tests pass.

### Storage

44. Storage audit reports exact before sizes.
45. Cleanup manifest removes only allowlisted Jarvis-owned reproducible/obsolete artifacts.
46. Current app/runtime/database/secrets/history/memory and required rollbacks remain.
47. Jarvis launches and core health passes after cleanup.
48. Exact freed space and retained footprint are recorded.

---

## 23. Delivery and finish condition

Leave:

- polished installed `/Applications/Jarvis.app`;
- one coherent Hermes backend/runtime;
- persistent conversation/session support;
- repaired personal Calendar and Gmail-draft actions;
- Jarvis UX 2.0;
- Today, Inbox, Project Cockpit, meetings, Focus, Teach Jarvis, Radars, Decision Journal;
- capability health/self-repair;
- safe storage-cleanup script and audit;
- current architecture/permission/action documentation;
- visible acceptance evidence;
- performance/resource/storage report;
- rollback/uninstall instructions;
- coherent commits and guarded private push;
- clean or fully explained working tree.

Do not stop at “implemented” or “tests passed.” Completion means the exact installed product is pleasant and reliable in ordinary use.

### Final owner report

End with no more than twelve concise lines:

1. what was repaired;
2. what changed in the UI;
3. how conversations now work;
4. how voice now ends and interrupts;
5. how context is inferred;
6. which personal actions now work;
7. what Today/Inbox/Projects add;
8. how Teach Jarvis works;
9. what remains deliberately blocked;
10. exact app/build/runtime versions;
11. storage before/after and space freed;
12. the single genuine remaining limitation, if any.

