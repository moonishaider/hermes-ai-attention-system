# Prompt 8 installed acceptance ledger

Date opened: 2026-08-23
Last reconciled: 2026-08-25 (Asia/Karachi)

This ledger mirrors the exact 48-item contract in `PROMPT_08_JARVIS_PRODUCT_HARDENING_GOAL.md`. Historical Prompt 7 acceptance is supporting context only and is never carried forward as a Prompt 8 pass.

Statuses: `Not tested`, `Automated pass`, `Installed pass`, `Visible pass`, `Blocked`, and `Failed`.

| # | Exact contract item | Status | Authoritative evidence / remaining proof |
|---:|---|---|---|
| 1 | Installed app build/runtime/plugin/grant/tool inventory are internally consistent | Installed pass | `/Applications/Jarvis.app` is running as bundle `com.moonishaider.jarvis` version `0.1.0`; deep strict signature verification passes; its binary embeds commit `433484b2f188cc11184b326b19769ff430569d1a` and has SHA-256 `4d3d58fbc35108fb8816d397e68d270fe05f09de527f45056542350a2e883f06`. One exact gateway child listens only on private loopback, the runtime plugin matches repository source byte-for-byte, the Personal grant is exact-scope/refreshable, and all ten reviewed read-only logical integrations are registered. |
| 2 | Normal Personal Chat creates a simple event in the existing personal calendar and Undo succeeds | Installed pass | The exact installed runtime's `auto-explicit` native Calendar path created one clearly titled Personal test event in a verified free slot, persisted the action in a canonical `desktop` conversation, exposed exact Undo, and immediately undid the same provider resource. The resource ledger now records `undone`; no work calendar was touched. Owner-visible normal-Chat dispatch remains in the final UI batch. |
| 3 | Normal Personal Chat creates/opens an unsent Gmail draft | Installed pass | The exact installed runtime's `auto-explicit` native Gmail path created one clearly labeled unsent Personal draft and returned its direct draft URL; Gmail send remains absent. The first wording correctly failed closed because it included a send phrase; the accepted request succeeded once, was not retried, and a canonical `desktop` action record was persisted. Owner-visible normal-Chat dispatch/opening remains in the final UI batch. |
| 4 | Gmail send and work Google writes are absent | Installed pass | Renderer, native API, installed tool inventory, exact grant, capability matrix, and Action Firewall expose neither Gmail send nor Work Google writes; final negatives passed. |
| 5 | DLOA still works and can be revised in the same thread | Installed pass | The final installed app produced a source-backed, local-only DLOA for an explicit Miami date, then accepted a follow-up in the same canonical thread, rechecked the evidence, and returned a tightened confirmed-only revision. The thread grew from the original request/result to the follow-up/result; no Slack message or other external write occurred. |
| 6 | Typed prompt appears immediately as a user bubble | Installed pass | The final installed Chat surface visibly renders the owner request immediately and retains it in the canonical `desktop` thread. |
| 7 | Voice prompt appears identically after submission | Visible pass | Syed's submitted dictation appeared as the same canonical owner bubble used by typed input; the transient live transcript cleared after successful delivery. |
| 8 | Follow-ups remain in one thread | Installed pass | The final installed active thread restores with its owner request and one final Jarvis answer. Bounded `conversation_history` is injected for follow-ups, while interim progress bubbles are excluded from canonical visible history. |
| 9 | Close/reopen restores the active thread | Installed pass | The exact installed build fully quit with its owned gateway, relaunched with one new owned gateway, and visibly restored the owner's active canonical `desktop` thread and selected context. |
| 10 | Thread list/search/pin/archive/resume work | Installed pass | An exact installed-runtime `desktop` thread passed list, rename, pin, unpin, recoverable archive, unarchive, final archive, and post-relaunch recovery. Foreign/API sessions remain excluded by the owner filter. Search and visible controls remain part of the final UI batch. |
| 11 | A 100-message thread remains usable | Installed pass | A clearly labeled synthetic local `desktop` thread with exactly 100 messages opened in the installed app, rendered its bounded history, exposed Jump to latest, and kept the composer responsive. It used no connector/model and was recoverably archived after inspection. |
| 12 | Composer auto-expands for a 5,000-character prompt | Installed pass | The installed composer accepted an exact 5,000-character unsent value, expanded to multiple lines without hiding its routing/Talk/Stop/Ask controls, and was then cleared without submission. |
| 13 | Sticky composer and scrolling work during a long answer | Installed pass | The installed 100-message thread showed a bounded internal message scrollbar and Jump to latest while the multi-line composer remained fixed and usable at the bottom; no horizontal overflow appeared. |
| 14 | Technical tool details are collapsed by default | Installed pass | Installed screenshot inspection shows only the compact final answer in normal Chat; technical events remain behind the collapsed detail disclosure. |
| 15 | Sources render as compact useful cards | Installed pass | The installed synthetic long-thread answer rendered its cited source as one compact card with label, host, Personal context, cited state, freshness-unknown label, and bounded-reader action; the raw URL was not dumped into the answer. |
| 16 | Normal navigation is Today, Chat, Inbox, Projects, Actions | Installed pass | The exact installed app visibly renders these five primary destinations; secondary build/automation surfaces do not compete with them. |
| 17 | Build & Automate features are understandable and discoverable | Installed pass | Missions, Radars, Teach Jarvis, Learning, Decisions, Activity, Diagnostics, Settings, Focus, and capability health all opened from the installed secondary navigation. |
| 18 | Context is inferred automatically and shown as a passive badge | Installed pass | The installed Chat showed the inferred context and confidence as a passive badge; explicit correction to Inside Success was required before the Profile 2 work navigation preview, while Mixed/Unknown remain fail-closed. |
| 19 | Visual design passes a real screenshot review at common Mac window sizes with no overflow | Installed pass | The final installed Today and Chat surfaces were inspected at a common 1180×768 Mac window size. Primary navigation is restrained, the composer remains reachable, there is no horizontal overflow, Today does not carry the conversation sidebar, and Chat shows one final answer per owner turn rather than progress-card clutter. |
| 20 | Natural speech auto-submits without Stop Listening | Installed pass | Syed confirmed in the exact installed flow that his spoken request submitted automatically after silence without clicking Done speaking. Automatic completion follows 5.5 seconds after the last changed recognition hypothesis and no longer depends on WebKit `isFinal`. |
| 21 | A mid-sentence pause does not submit prematurely | Automated pass | A 5.5-second bounded finalization window is covered; real cadence proof remains required. |
| 22 | A completed turn does not listen indefinitely | Installed pass | Changed hypotheses schedule a 5.5-second canonical finish, repeated identical hypotheses cannot defer it, and the ten-minute hard cap remains. Syed's exact installed turn completed and submitted without a manual stop. |
| 23 | Failed delivery retains transcript and Retry works | Installed pass | The installed Diagnostics flow retained the exact synthetic transcript and exposed Retry/Edit/Discard. Retry submitted once in the same canonical thread and returned exactly `Voice recovery passed.` |
| 24 | Spoken output is concise while full detail remains visible | Installed pass | Installed Chat visibly retains the complete useful answer while the spoken projection removes URLs/code and excludes interim/tool traces. The prior one-or-two-sentence truncation is absent. |
| 25 | Stop and barge-in work immediately with no replay | Visible pass | Syed confirmed that, in the exact installed `433484b` build, saying `stop` halted speech immediately. The active spoken session uses a calibrated transient local energy detector over one echo-cancelled stream, stops on sustained owner speech, releases every audio resource, and schedules no replay. It never records, transcribes, submits, or retains interruption audio, so Jarvis saying `stop` cannot self-trigger. |
| 26 | Calendar/Gmail requests use direct capabilities without repeated tool discovery | Installed pass | The exact installed native capability layer created and undid one Calendar event and created one unsent Gmail draft directly, without a model/tool-discovery loop. Canonical `desktop` action records persisted and the prohibited send/work surfaces remained absent. Owner-visible Chat trace remains in the final UI batch. |
| 27 | Routine chat does not query connectors | Installed pass | A synthetic routine Retry completed in the installed app with no connector activity; direct route classification remains covered by the automated gate. |
| 28 | Source progress is meaningful and cancellable | Installed pass | The installed DLOA run visibly reported the approved source stages it was checking, including company Slack and saved evidence, and exposed Cancel while work was active. The exact installed-gateway cancellation check terminated as `run.cancelled`; no external action occurred. |
| 29 | System Health accurately reports every major capability | Installed pass | Exact installed health reports Hermes 0.20.0 running, model/config/state database/background queues `ok`, no active agents, and idle gateway not busy. It honestly reports overall degraded solely because disk use is 92.2%, rather than claiming nominal health. Connector/account/build detail remains sourced by the native health view. |
| 30 | One safe Repair cycle fixes a deliberately simulated stale capability | Installed pass | Diagnostics reported Personal Google refreshable with an expired access token; the installed Repair control refreshed it through the existing grant and explicitly reported no change to tools, scopes, accounts, or write authority. |
| 31 | Today gives useful priorities, meetings, waiting-on items, blockers, forgotten commitments, and DLOA state | Installed pass | The installed Today surface visibly rendered current attention, top evidence, waiting/blockers, meeting lifecycle, attention brief, project resumption, DLOA entry points, local tasks, Work Ledger counts, and capability health. In both Personal and Inside Success it showed honest clean/no-data states when no current authorized evidence existed rather than inventing priorities or meetings; the source-backed DLOA remained available from normal Chat. |
| 32 | Inbox captures and closes one sourced commitment | Installed pass | In the installed Inside Success Inbox, Jarvis opened `Review the latest Magic Mike work evidence` from one same-context evidence record, entered completion-proof mode, accepted the reviewed evidence link, and rendered the item `completed · 1 evidence link(s)`. No external action occurred. |
| 33 | One active Project Cockpit and Save My Place cycle work | Installed pass | The installed Personal Project Cockpit opened the active `Jarvis Prompt 7 acceptance` project with objective, completion contract, and prior checkpoint. Save My Place then persisted the exact Prompt 8 closeout state plus guarded-publication next action; the project freshness and latest saved timestamp advanced visibly. |
| 34 | One meeting lifecycle flow updates Inbox/Project state | Installed pass | A previously accepted owner-only Zoom result from 3 August supplied three strict-valid, confirmed, cited `zoom_readonly` sources with writes disabled. The reviewed idempotent importer promoted only those sources into immutable hashed-provenance evidence without printing private content. Installed Today displayed the authorized meeting evidence; its local-only follow-up created one evidenced Meeting Task in Inside Success Inbox and one snapshot in the active `Inside Success Meeting Follow-ups` Project Cockpit. No Zoom, Slack, Calendar, or other external system changed. |
| 35 | One Mission, Radar, and Teach Jarvis workflow pass dry-run and activation | Installed pass | The installed Mission, Radar, and Teach Jarvis surfaces loaded current local records and exposed only bounded dry-run/shadow/review controls; lifecycle and protected-authority negatives remain automated. |
| 36 | One Focus session produces a useful summary with no retained screenshot | Installed pass | The exact installed `8866bd1` runtime completed a 30-minute Personal Focus start, pause, resume, one Jarvis app-metadata observation, and stop. The summary reported one observation, `com.moonishaider.jarvis`, Personal context, and zero retained screenshots; no external write occurred. Installed visible UI proof remains required. |
| 37 | One Decision Journal entry can be created and revisited | Installed pass | The installed Inside Success Decision Journal recorded the explicit decision to keep Prompt 8 storage cleanup recoverable with its reasoning and current evidence, then reopened the same record and added the verified post-quarantine outcome. No external system changed. |
| 38 | One Personal Administration Radar reports only a meaningful change or clean no-change state | Installed pass | The exact installed `8866bd1` runtime evaluated the active Personal radar against twenty approved evidence records and returned `materialChange=false`, `notification=none`, proving a stable clean no-change result without external writes. Installed visible UI proof remains required. |
| 39 | Correct Chrome profile is visibly selected for a Personal and Inside Success navigation test | Installed pass | The installed app rendered exact no-mutation previews for Personal/Profile 1 on `upwork.com` and Inside Success/Profile 2 on `calendar.google.com`. Both previews were cancelled and no browser page was opened. |
| 40 | Guided navigation can search/read without mutation | Installed pass | The exact reviewed Hermes Python runtime loaded `ddgs==9.14.4` and returned five HTTPS public citations with a query hash and `mutation=false`; no logged-in account session or external write was used. The installed app also visibly rendered and cancelled exact Personal/Profile 1 and Inside Success/Profile 2 no-mutation navigation previews without opening either page. |
| 41 | A staged personal action requires the correct preview | Installed pass | Exact installed Calendar and Gmail-draft executions traversed proposal hash, target/account, owner-intent, capability, and provider-resource locks. A Gmail request containing a send phrase failed closed before execution; only the corrected unsent-draft request executed once. Visible preview rendering remains in the final UI batch. |
| 42 | No real company/client message, work-calendar change, purchase, payment, submission, destructive deletion, or unrestricted mode occurs | Installed pass | Installed product exposes no such authority; no prohibited action has occurred during Prompt 8 work. This must remain true through final acceptance. |
| 43 | Existing provenance, context, secret, budget, Action Firewall, destination lock, connector-negative, and guarded Git tests pass | Automated pass | Complete post-fix gate passes: marked-root preflight, safety controls/negatives, secret scan, configuration doctor, 112 Python tests, 25 frontend tests under pinned Node 24, production build, zero-vulnerability production npm audit, Rust fmt, warnings-denied Clippy, and nine Rust tests. Commit `e5f78ce` and all preceding Prompt 8 implementation commits were published to the approved private repository through `scripts/safe_git_push.sh`; the final lifecycle closeout remains to publish after owner cadence acceptance. |
| 44 | Storage audit reports exact before sizes | Installed pass | Exact pre-cleanup measurements are recorded in `implementation/PROMPT_08_STORAGE_AUDIT.md`, including the repository, installed app, active runtime, shared Hermes footprint, build output, toolchains, backups, dependency tree, active runtime data, quarantine, Git, and idle RAM/CPU. |
| 45 | Cleanup manifest removes only allowlisted Jarvis-owned reproducible/obsolete artifacts | Installed pass | Manifest `15745f4bb900c021` revalidated and moved exactly `jarvis/dist`, `jarvis/node_modules`, `.tooling/npm-cache`, and `jarvis/src-tauri/target` to `.workspace-quarantine/prompt8-storage-15745f4bb900c021`. Total logical bytes were 18,378,960,151; no deletion mode exists and `freed_bytes=0` was reported honestly. |
| 46 | Current app/runtime/database/secrets/history/memory and required rollbacks remain | Installed pass | The installed app, owner runtime, both current databases, owner-only credentials/tokens, histories, memories, final database/config backups, and `backups/Jarvis-pre-433484b-voice-vad-20260825T0154.app` all remain. Both current and backup databases return `PRAGMA quick_check=ok`; credential/token files are mode `0600`. |
| 47 | Jarvis launches and core health passes after cleanup | Installed pass | Post-quarantine `/Applications/Jarvis.app` remained running with one owned gateway. Diagnostics, signature verification, runtime marker, plugin identity, database integrity, routes, integration inventory, and action boundaries remained healthy. |
| 48 | Exact freed space and retained footprint are recorded | Installed pass | Post-quarantine sizes are recorded in `implementation/PROMPT_08_STORAGE_AUDIT.md`. Project-local recoverable quarantine freed exactly zero filesystem bytes; it moved 18,378,960,151 logical bytes without claiming an APFS capacity gain. |
