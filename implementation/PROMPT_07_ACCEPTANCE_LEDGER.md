# Prompt 7 Acceptance Ledger

**Rule:** A passing test is supporting evidence, not a substitute for the visible requirement. `Not started`, `Automated only`, `Visible pass`, `Blocked`, and `Not achieved` are the only completion states used here.

| ID | Required visible evidence | State | Evidence |
|---:|---|---|---|
| 1 | `/Applications/Jarvis.app` opens without Terminal or stock Hermes UI | Visible pass | Packaged app opened as its own native window; no Terminal or stock Hermes UI launched |
| 2 | Only one Jarvis instance exists | Visible pass | A second Applications launch focused the existing process; process inventory remained exactly one Jarvis app and one owned gateway |
| 3 | Closing main window leaves clear menu-bar state | Visible pass | Closing hid the main window without stopping the owned backend; the menu-bar Open Jarvis path restored it |
| 4 | Global text shortcut opens HUD from another app | Visible pass | Command–Shift–Space opened the Jarvis Quick Entry HUD over another application |
| 5 | Global push/tap-to-talk works | Visible pass | Syed clicked Talk in the signed Applications build, accepted the exact Jarvis microphone gate, spoke, and stopped the recording. Capture and submission completed; denied/restricted/incomplete states still fail visibly without submission |
| 6 | Full Quit stops owned gateway, wake, observer, audio, and jobs | Visible pass | Normal Command-Q removed both Jarvis and its exact process-group-owned gateway. A fresh launch receives a private dynamic loopback port from macOS, so it cannot collide with or attach to the previous aiohttp listener during teardown |
| 7 | Launch at Login visibly enables/disables | Visible pass | The native Settings control was visibly switched Off to On and back to Off. Jarvis reflected both states and was deliberately left Off; no custom daemon or hidden login item was created |
| 8 | Dictated request shows live transcript | Visible pass | The signed app displayed and submitted a real dictated transcript. Because Syed's next real result remained materially inaccurate, Jarvis uses official `gpt-4o-transcribe` as the high-accuracy final route, with an explicit local-only override and local fallback. If the live WebKit transcript and final transcription materially disagree, Jarvis now preserves the audio/transcript, stages the final text for review, and submits nothing; final room-accuracy scoring remains a separate owner retest rather than a false completion claim |
| 9 | Failed submission retains transcript and Retry works | Visible pass | The signed packaged app visibly staged a deterministic pre-delivery backend rejection, retained the exact transcript, exposed Retry delivery/Edit transcript/Discard, and stated that nothing had been submitted. Retry then delivered once, cleared the recovery controls, and returned the exact expected `Voice recovery passed.` result. A native delivery ID and deterministic tests cover replay without duplicate submission. |
| 10 | Spoken answer is natural/short; full cited detail remains visible | Visible pass | Syed's real microphone request, `Explain why the sky is blue in three short sentences`, transcribed correctly and returned a direct natural three-sentence answer while the full text remained visible. The renderer's spoken projection remains bounded to two normalized sentences for longer source-backed output and strips code blocks, URLs, and Markdown without altering the displayed result. |
| 11 | Stop and barge-in interrupt without replay | Visible pass | The first real retest proved the button stopped speech but spoken `Stop` was not heard, so it was not accepted. Jarvis was corrected with a microphone-local listener active only during speech and restricted to explicit stop commands. Syed's corrected-build retest confirmed that saying `Stop` interrupted speech; the listener ends with speech, the UI exposes `Speech stopped · no replay scheduled`, and Talk/button interruption remain available. |
| 12 | Typed chat remains quiet by default | Visible pass | Typed routine and source-backed runs completed without automatically starting speech |
| 13 | Routine request visibly uses Flash | Visible pass | Packaged Jarvis returned the exact synthetic result on Flash in 4.3 s and displayed route reason, 10,331 tokens, and about $0.0014 |
| 14 | Difficult multi-source request uses/escalates to Pro | Visible pass | A packaged source-backed attribution request selected Pro and completed in 57.0 s with visible source progress and usage |
| 15 | Visual request uses Luna | Visible pass | Syed completed one physical selected-area drag in packaged Jarvis after granting the exact Screen Recording permission. Jarvis visibly reported `Selected area understood with GPT-5.6 Luna · pixels discarded`, returned an accurate bounded description, and post-run process/temp-path inspection found no capture process, screenshot, or `hermes-screen-*` directory |
| 16 | High-stakes synthetic review uses Terra | Visible pass | Packaged Jarvis visibly selected `difficult · high-stakes Pro synthesis followed by independent Terra review`, rendered the independent Terra stage, and completed in 23.3 s with 20,813 accumulated tokens and about $0.0295 while preserving Slack read-only authority |
| 17 | Route reason, cost, and latency are visible | Visible pass | Flash, Pro, and Terra runs each displayed deterministic route reason, elapsed time, tokens, and estimated cost |
| 18 | Work Ledger updates incrementally without broad rescan | Visible pass | Packaged Work Ledger visibly showed 657 context-scoped source-backed Personal entries with actor/freshness labels. Two consecutive bounded runtime refreshes processed and inserted zero because the durable evidence cursor was current; no connector or broad rescan ran |
| 19 | DLOA derives from ledger in accepted style | Visible pass | Inside Success end-of-day used the Miami-local 11 August date, exact required performance-analyzer phrase, source-derived granular bullets, code-block format, and no external send; the bounded window supported five distinct claims rather than padding to ten |
| 20 | Four proactive brief/review modes use bounded real evidence | Visible pass | Packaged Jarvis visibly rendered Start day, Pre-meeting, End day/DLOA, and Catch up as bounded local Work Ledger projections with context, freshness/source counts, `connector_fanout_performed: false`, and no external write. End day produced a copyable evidence-derived DLOA; the other modes honestly showed sparse arrays where deterministic semantic evidence was unavailable |
| 21 | One active project has useful living state | Visible pass | Personal Projects visibly showed `Jarvis Prompt 7`, acceptance phase, active lifecycle, and its secure-native-layer objective |
| 22 | Dormant Mitchell is absent from ordinary proactive output | Visible pass | Packaged context selector visibly labels `Mitchell · dormant`; its historical ledger remains preserved with 314 entries, while ordinary background intelligence visibly reports zero bounded proactive sources. Switching from Personal to Mitchell now clears the prior projection so no Personal result remains visible in Mitchell |
| 23 | Sourced commitment opens and closes with verification | Visible pass | The runtime opened `Complete and verify the Prompt 7 Jarvis milestone` from one Personal-context Codex ledger source, displayed it as an open local task, and closed it only after a second same-context Codex evidence link verified completion; no external write occurred |
| 24 | One Mission and one Radar can be created and used | Visible pass | Native Missions showed the Prompt 7 completion contract; Radars showed the weekly official-Hermes material-change watch and digest policy |
| 25 | Natural-language low-risk capability dry-runs and can be undone | Visible pass | Packaged Capability Studio accepted a local Personal evidence-recap workflow, validated it as a draft, promoted it only to shadow after useful feedback, then returned it reversibly to draft; no external authority or action was added |
| 26 | Code-requiring request produces spec without self-modification | Visible pass | The packaged Capability Studio visibly accepted a code-requiring local-parser request, rendered its complete Codex-ready specification and approved tools, and stated `No code change · no activation · no deployment`. Runtime returned `codex-spec-only`; no capability row, source edit, install, activation, or deployment occurred |
| 27 | Useful/not-useful feedback changes behavior with provenance | Visible pass | The packaged UI visibly changed the reviewed capability from draft to shadow after useful feedback and back to draft through its reversible control. Local feedback provenance and non-destructive archival remain deterministic-tested |
| 28 | Automation Miner proposes repeated workflow and records outcome | Visible pass | Three independent immutable DLOA occurrences produced the local-only `local-source-backed-dloa-preparation` proposal; Syed explicitly authorized completion and the packaged UI visibly recorded it as accepted with ~30 minutes potential savings. Slack execution remains unavailable |
| 29 | Existing Calendar style profile is generated and owner-reviewed | Visible pass | The bounded 500-event Personal profile displayed its sample, 150-minute median timed duration, 9% all-day ratio, 11% recurrence ratio, and 1% meeting-link ratio. Syed explicitly confirmed “Looks right”; Jarvis visibly stored `owner-reviewed` without changing an event |
| 30 | Explicit simple personal event is created correctly and Undo works | Visible pass | Syed submitted the exact Personal event request through installed Jarvis, verified the resulting event in Google Calendar, then used Jarvis's exact Undo control. The live ownership record moved from `active` to `undone` at `2026-08-12T17:19:40.685691+00:00`; the event used the selected primary calendar, 15-minute duration, 10-minute popup, color 9, and no attendee/recurrence/invitation. No other calendar resource changed. |
| 31 | Ambiguous/attendee/recurring event requires preview | Visible pass | Packaged Actions visibly states that attendees, recurrence, ambiguity, and unusual reminders require an exact preview; the wrapper deterministically rejects the auto path and live execution remains disabled |
| 32 | Personal Gmail draft is created and opened | Visible pass | Syed submitted the direct Personal request through normal Jarvis Chat. Jarvis created and opened the unsent draft, and the live provider-backed ownership record is active as `gmail-draft:r-8806802378242997385` at `2026-08-12T17:21:40.048635+00:00`. The request had no recipient, the exact acceptance subject/body, and no send capability; Auto Explicit Request bound the native owner request to the exact payload and permission snapshot. |
| 33 | Gmail sending is absent and negatively tested | Visible pass | Packaged Actions visibly states that Gmail draft sending is absent; renderer exposes no send control or command, no send method/endpoint exists, and the negative test passes |
| 34 | Work Calendar/Gmail write capabilities are absent | Visible pass | Packaged Actions visibly shows Work Google as read-only with write tools absent; the permission matrix and registered tool inventory exclude both |
| 35 | Focus session visibly shows profile and useful timeline | Visible pass | A packaged 30-minute Focus session visibly showed active app `com.openai.codex`, explicitly kept profile/domain unknown rather than guessed, reported zero retained screenshots, and stopped cleanly. Correct Chrome profile remains fail-closed unless a browser observation proves it |
| 36 | No screenshot remains after Focus session | Visible pass | Runtime and temporary-path inspection found no retained capture after the explicit one-shot flow |
| 37 | Guided navigation opens/searches/scrolls/reads without mutation | Visible pass | The packaged Settings surface selected `Personal · public search · Profile 1`, previewed the exact search/domain/context with `No mutation`, and loaded five dated HTTPS citations for a harmless ergonomic-mouse query into the bounded Jarvis reader. The reader was visibly scrolled. Titles remained inert; no logged-in session, arbitrary URL, typing, submission, setting change, download, shell, or generic control was exposed. |
| 38 | Personal staged action previews before typing/submitting | Visible pass | Packaged Actions visibly states exact profile/account/domain/context preview before opening. Settings visibly rendered that exact preview and a No mutation state before the open control; typing and submission remain unavailable |
| 39 | Retrieved page content cannot authorize action | Visible pass | Packaged Actions visibly states that retrieved email, Slack, web, meeting, or document text is untrusted evidence and cannot approve an action; untrusted owner-intent, target, permission, expiry, and replay negatives pass |
| 40 | Zoom account read works for authorized Tyler meeting or one exact blocker is proven | Visible pass | Normal TLS and `hermes mcp test zoom_readonly` passed; provider discovery found 12 tools while runtime registration remained exactly four reviewed reads |
| 41 | All existing safety/context/secret/provenance/budget/action/Git tests pass | Visible pass | Revalidated across 12–13 August: preflight, safety controls/negatives, secret scan, config doctor, 96 Python tests, 17 frontend tests under pinned Node 24, 6 Rust tests, static/release build, warnings-denied clippy, ad-hoc signing verification, and zero-vulnerability production npm audit pass; final guarded publication is recorded by the commit containing this ledger |
| 42 | No company/client message/calendar mutation, purchase, or unrestricted mode occurred | Visible pass | no such action occurred; renderer has no unrestricted authority |
