# Current Operational State

**Authoritative as of:** 6 August 2026

**Prompt 6 state:** Hermes Agent 0.20.0 / official Desktop migration accepted

**Prompt 4 rollback checkpoints:** milestone start `015948b`; pre-closeout `ac43070`; pre-Google-refresh `bf04f22`

**Operational implementation through:** `116a86f` (`Resolve Google hourly OAuth expiry`); later handoff-only commits do not change runtime state

**Purpose:** This is the single current source of truth. Historical milestone records are evidence of what was true when written, not the present status.

## Evidence levels

- **Live and acceptance-tested:** exercised against bounded real data with source/context and leakage checks.
- **Live but metadata-smoked:** authenticated inventory or metadata probes passed; real usefulness is not yet accepted.
- **Implemented locally, awaiting real acceptance:** code and deterministic tests pass; a supervised real test remains.
- **Shadow/preview only:** deliberately unable to perform the external action in normal Hermes runtime.
- **Externally blocked:** prepared but waiting on a provider, credential, permission, or official artifact.
- **Intentionally deferred:** excluded for safety or lack of demonstrated need.

## Current status

| Area | Evidence level | Current truth |
|---|---|---|
| Safety and repository | Live and acceptance-tested | Marked root, hooks/rules, preflight, command negatives, config doctor, secret scan, backup/restore, and 62 tests pass. Private remote is `moonishaider/hermes-ai-attention-system`. Prompt 6 began from clean commit `2ae9512` with rollback tag `prompt6-pre-desktop-upgrade-20260805`; this dated record ships with the coherent Prompt 6 implementation commit. |
| Daily launch and health | Native Desktop live and acceptance-tested | `/Applications/Hermes.app` is the primary path and starts the production Hermes backend without Terminal. Native Quick Entry opened from another app with Command+Shift+Space before and after the main window was closed; Attention opens with Command+Shift+A. Command+Q removed app/backend/wake/audio processes, and clean relaunch passed. The project plugin shows context, safe status, tasks, screen view, cancel/stop, learning, and preview state. The old launcher/TUI/Tk overlay are diagnostic fallbacks only; Finder cwd is fixed. No custom daemon, development server, login item, or launch agent exists. |
| Model routes | Live and representative-task tested | Flash, Pro, Luna, and Terra each passed bounded representative tasks. Flash and Luna tied on deterministic routine quality; Luna was slightly faster in the tiny sample but about 14x more expensive, so Flash remains default. Sol remains builder-only. |
| GitHub personal/company | Live and acceptance-tested for bounded retrieval | Separate `/readonly` connections and 14-tool allowlists remain intact. Personal GitHub participated in two accepted project-resumption runs; company GitHub participated in an accepted source-backed daily-report draft. Write tools remain unavailable. |
| Slack Inside Success/Mitchell | Live and acceptance-tested for bounded retrieval | Both strict read-only connections participated in accepted cross-context use. A new focused Mitchell run passed with 8/8 fully resolved claims over 10 sources. A new Inside Success retry timed out fail-closed, so handoff and commitment checks use deterministic composition of the already accepted same-day Inside Success evidence plus the accepted Mitchell result. No Slack write tool exists and no message was sent. |
| Google work | Live, refreshable, and acceptance-tested | One account-level offline grant contains exactly Gmail read-only, Drive read-only, Calendar-list read-only, and Calendar-events read-only. Owner-only token storage, backup, forced refresh, health reporting, and live Gmail/Drive/Calendar metadata smokes pass. The Workspace MCP preview rejected the combined resource grant, so its three endpoints are disabled and three host-locked standard-API read tools are exposed instead. Prior Inside Success briefing and attribution acceptance remains valid. |
| Google personal | Live, refreshable, and acceptance-tested | The personal app is In production; Google verification remains optional for this private single-user deployment, though an unverified-app warning can appear. One offline grant for `moonishaider12@gmail.com` contains exactly the same four read-only scopes. Google issued no refresh-token lifetime field; owner-only storage, backup, forced refresh, health reporting, and live Gmail/Drive/Calendar metadata smokes pass. Three host-locked standard-API tools remain context-isolated to personal. |
| Codex history | Live and acceptance-tested | 187 source files, 64,500 checkpointed lines, bounded ingestion, redaction, tool-output exclusion, and project-resumption retrieval pass. Deterministic calibration reduced unknown from 49.1244% to 47.0728% by reclassifying 198 records from one verified workspace mapping. Genuine ambiguity remains unknown. |
| ChatGPT history | Live and acceptance-tested for official backfill | The user-selected 308.8 MB official ZIP contained five contiguous `conversations-NNN.json` shards. Preview selected 47 of 458 conversations from 1 March onward at about 120 MiB maximum RSS. Exact approval imported 47 records; a rerun reported 47 duplicates, all 47 provenance records validated, and source-backed retrieval passed without printing content. The records remain evidence, with 46 inferred and 1 uncertain; all remain `unknown` pending semantic calibration. No continuous account-sync API is claimed. |
| Zoom | Live and acceptance-tested for bounded retrieval | `Hermes Work Zoom Read Only` is a user-managed Profile 2 app using secretless public-client PKCE. The grant contains exactly the four reviewed meeting/recording read scopes, shared-access widening stayed unchecked, and owner-only refreshable token state is active. Live discovery returned 12 provider tools; Hermes exposes only four reviewed reads and excludes the two observed Canvas/Hub writes. A bounded 1–4 August case retrieved three recent work-meeting evidence references: 3/3 claims cited and confirmed, no reported leakage, 65.5 s latency, and $0.00523 estimated model cost. |
| Attention, handoffs, reports | Live and acceptance-tested for read-only daily use | Cross-context search, context-switch handoff, bounded commitment/contradiction detection, project resumption, a source-backed daily-report draft, work briefing, same-day work attribution, recent Zoom retrieval, and personal obligations pass. Slow connector joins are composed locally only from strict-valid accepted inputs. Slack publishing remains shadow-only. |
| Specialists and memory | Implemented and acceptance-tested | Seven deterministic controls pass for persistent loading, context restrictions, report-publish prohibition, namespace/context-scoped memory, disabled serious-mode tax/finance review, and payment prohibition. Tax/finance remains disabled. |
| Voice and activation | Native and acceptance-tested | Real microphone, local STT, Flash, Ryan Edge TTS, and local `Hey Jarvis` detection pass. Typed chat stays quiet. Microphone turns speak a short natural prefix while detail/citations remain visible. Syed accepted the revised calendar reply and quality-preserving latency. Native Stop and permanent Attention Stop speaking halt audio immediately; command-palette Stop and Control+Shift+S are installed. The visible wake switch persisted Off, and repeated wake phrases caused no recording/response. Very short phrases can still be misheard. |
| Screen viewing | Native Attention live and acceptance-tested | After the one-time macOS permission restart, Syed used Attention in Personal context to select one region. Luna returned a bounded description, and no screenshot remained in temporary or project runtime storage. The backend exposes no arbitrary path, retention, browser, or computer-control endpoint. |
| Personalization and learning | Native and acceptance-tested | Concise `SOUL.md` and stable-facts-only `USER.md` are installed; context/source evidence remains separate. Hermes learned an explicit spoken-answer preference without Codex and stored it in Syed's stable profile. Attention now shows a plain item count/recent label plus an advanced graph link. Memory/skill writes remain review-gated for non-routine changes; curator archival is recoverable and cannot prune bundled skills. |
| Inside Success daily publish | Destination-locked, preview-only | Syed selected `#sd-dloa-tyler`; the immutable lock is workspace `T01K1TNLXLK` / channel `C0B0RT26KCZ`. Six prior DLOAs informed formatting. The current private v2 preview contains four confirmed Syed claims backed by four validated company Slack permalinks; unresolved and uncertain claims were omitted. Wrong destination, broad mentions, expiry, replay, and kill-switch paths are tested. The preview expired safely, the sender remains disconnected, no generic send tool is exposed, and no message was sent. |
| Gemini history | Official export requested; delivery/import pending | Syed completed the personal Google Takeout selection and requested the export. When the ZIP arrives, Hermes must preview its schema, count, size, and date range before one explicit import confirmation. The importer is not yet implemented because the real archive format has not been inspected. No continuous Gemini-history API is claimed. |
| Safe web/shopping research | Live and acceptance-tested for bounded public research | Pinned `ddgs==9.14.4` search plus guarded public-page fetch return URLs/dates/hashes, flag prompt injection, redact secrets, and block local/credential URLs. A Logitech research smoke returned six cited results and safely fetched one official page. No browser session, cart, checkout, payment, or background browsing exists. |
| Background availability | Native lifecycle acceptance-tested; launch-at-login off | Closing the window left Quick Entry available. Command+Q stopped the app, backend, wake listener, and audio process, and relaunch succeeded. No custom persistent service, launch agent, or hidden login item exists. Launch at Login remains an unselected native option. Wake is currently off. |

## Prompt 5 first-use responsiveness

One bounded project-resumption task was run through the real Hermes runtime over the 26 July–4 August window. It accepted 7/7 cited claims, resolved 18/18 claim references across 15 sources, reported no leakage, confirmed writes disabled, used DeepSeek V4 Flash, cost approximately $0.01115, and completed in 178.9 seconds. This supports an honest one-to-three-minute expectation for multi-source work. No caching, timeout, or orchestration change was made because the run succeeded at the existing freshness and citation standard; the UI provided immediate working/tool progress rather than a silent wait.

## Acceptance result in one sentence

Hermes can currently save time through the native Desktop on source-backed project resumption, cross-context evidence gathering and handoff, bounded commitment review, Inside Success briefing and work attribution, Mitchell open-loop review, recent Zoom-meeting retrieval, personal obligations, official ChatGPT backfill retrieval, report drafting, public product research, natural short spoken interaction, explicit one-shot screen interpretation, and bounded native preference learning. Prompt 6 migrated Desktop acceptance is complete; Slack publishing remains intentionally unsent and shadow-only.

## Non-negotiable boundaries

No local development server, unrestricted browser/computer control, YOLO mode, generic Slack send, broad filesystem tool, account/admin mutation, company/client write, silent OAuth widening, TLS bypass, payment/checkout, permanent deletion, or launch agent is enabled. Retrieved content is untrusted evidence and must retain source, account, context, date, and confidence labels.
