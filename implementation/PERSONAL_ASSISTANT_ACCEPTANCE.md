# Goal 09 implementation and acceptance snapshot

This document records installed and isolated acceptance for the local personal-assistant milestone without publishing private receipts, account identities, conversations or source records. Conditional capabilities and observed limitations are listed explicitly; local builds and fixtures do not establish that every installed journey works.

## Observed in the installed application

- Neutral conversations, retained history, attachment isolation and one cancelled streaming response with a labelled canonical partial. Two independent native requests were also observed running simultaneously.
- Multi-format document analysis, honest corrupt-file handling and resistance to instructions embedded in test documents. Image/scanned-document vision was exercised; native image copy/paste preserved decoded pixels without an additional model call. This does not establish every OCR or drag/drop path.
- Real editable outputs opened in native document applications; Save As cancellation and a byte-identical saved copy were checked.
- A synthetic financial reconciliation included all selected input rows, retained ambiguous items and produced independently checked spreadsheet and document artifacts. A subsequent tax-preparation request completed primary and reviewer stages with four artifacts; the reviewed PDF opened in the native viewer. It explicitly remained not ready to file, with no assessed liability. Reviewer outputs currently duplicate titles rather than link to the earlier artifact version; both are retained.
- Scoped personal calendar creation, corrected editing and exact undo were checked against provider readbacks. A draft remained unsent after body revision. Earlier failed attempts were retained as regression evidence.
- An explicit preference was saved and undone. A disposable skill revision was reviewed and rolled back; a subsequent native request read that saved skill and followed its procedure correctly. Unrelated pending memories were left untouched.
- A workflow executed a labelled synthetic dry run and a read-only shadow run; a tested active version then ran on an actual timer, produced a changed-result banner, and reported no notification for a subsequent unchanged check. The exact test job was cancelled. Simulation output is not represented as provider activity.
- A meeting-derived task was corrected in the native interface while retaining its original context, owner and source attribution. An explicitly scoped project and an unverified owner-reported checkpoint were created and resumed without inferring completion.
- Quick Entry was opened through its Settings launcher, displayed conversation files and accepted a request. Closing the window did not cancel that request. The keyboard shortcut requires separate verification.

## Fixture and source verification

Isolated checks cover run ownership and late events, canonical persistence recovery, exact retry identity, scheduler leases and replay, context boundaries, deterministic finance arithmetic, document retention, scoped permission denial, and reversible learning. These tests supplement installed evidence; they do not replace native UI or provider readbacks. A scoped generated-build cleanup helper passed eight isolated safety tests and a real manifest dry run; the exact reviewed manifest was subsequently applied, reclaiming approximately1.47GB of observed free space. Installed app/bundle hashes and database integrity checks remained unchanged/passing, with verified recovery sets retained.

## Remaining acceptance and conditional capabilities

Daily-report processing previously encountered incomplete output and recovery failures. A later installed grounded revision saved a new report version; independent review passed persistence, date-window, attribution and citation safeguards. A subsequent installed revision passed the requested seven edits and two focused context items, without adding prior background; it reused completed extraction. A subsequent live refresh independently passed grounding and persistence while retaining the accepted earlier version. The refreshed output used four owner bullets and two context items instead of the literal requested five owner bullets: redundant deadline emphasis was omitted, with no distinct task, date or deliverable lost in independent review. This remains a presentation limitation, not an exact-count pass. Earlier wording/grouping failures remain historical regression evidence. Successful transport or a saved version is not a complete quality pass. Earlier tax schema failures were superseded by a successful installed retry, while the artifact-version linkage limitation remains.

Final native lifecycle showed core readiness, the refreshed report and no recovery panel; scoped cleanup/accounting and recovery verification passed. Conditional input/voice checks remain distinct from these observed outcomes. Native computer control depends on the operating system's explicit permission and exact selected target. Builder computer tools do not prove the application's computer driver works. The operating-system Screen Recording switch was observed on, but current-binary capture was not exercised; the native driver's Accessibility permission remains off.

Wake support is optional and remains off during non-acoustic checks. Fresh-room voice and visual acceptance require the owner. The companion currently supports conditional authenticated reads; verified private HTTPS ingress, device access and remote chat are not claimed complete. No public tunnel is enabled by these checks.

No complete readiness or universal permission claim is made. Publication of this summary does not publish private test data or authorize provider actions, sends, filings, payments, or changes to operating-system permissions.

The latest refresh incorporated changed source evidence and reused retained work, with one new extraction plus composition and review. Explicit ISO and month-date parsing is covered by tests; ambiguous date bounds require clarification. Final cleanup/accounting passed. These measurements describe the exercised local workflows; they do not establish universal source coverage or model accuracy.

Unblocked implementation and installed acceptance work is complete at this checkpoint. The conditional operating-system, acoustic, remote-ingress and optional history-export limitations above, and the literal bullet-count presentation limitation, remain explicit.
