# Personal-assistant implementation and acceptance

This checkpoint preserves the earlier installed assistant and adds focused report, document, attention and scoped-browser repairs. Tests and installation are separate from native acceptance; the limitations below remain explicit.

## Existing behavior preserved

Installed evidence covers retained and concurrent conversations, cancellation with a canonical partial response, attachment isolation, editable document delivery, synthetic financial reconciliation, separate tax-preparation review, personal calendar changes with exact undo, an unsent revised Gmail draft, reversible preferences and skills, and actual changed/unchanged timer results. Tax outputs remain not ready to file. Company writes, message sending, payments and final filing remain unavailable.

## Focused continuation

- Daily-report revisions now validate explicit presentation counts separately from source grounding, preserve bounded revision context, and retain work relevance. A source refresh that replaces search rendering with the same authenticated message body can rebind previously verified facts only after exact body, author receipt, connection, account, timestamp and conversation checks. Original evidence remains intact.
- The installed corrected report contains five supported work communications and two separate meeting-context bullets. A repeated timing recommendation is explicitly a reiteration, not another completed deliverable. Collective billing wording, date boundaries, sources and attribution uncertainty remain intact; sports and prior-period background are excluded.
- The corrected sample took 32.1 seconds: 117,932 input plus 983 output tokens across two provider calls, with zero new extraction calls and zero cached input reported. Estimated model cost was $0.07689154. Aggregate tokens count repeated context across calls, not unique evidence. Historical extraction metadata is excluded from this turn's cost. These are sample measurements, not guarantees.
- Generated documents carry explicit parent, root, version, source and reviewer metadata. Native PDF revision and latest/earlier cards passed and survived relaunch. Unrelated same-title files never merge automatically; earlier finance outputs are preserved.
- Native multi-file selection, unsupported/encrypted/corrupt feedback and scanned-PDF vision fallback passed. Existing clipboard-image evidence remains valid. Small/full windows, long conversation and composer, preview and keyboard checks were inspected in the packaged app.
- Due reminders use durable occurrence identities. Native delivery, ten-minute snooze, suppression across restart, due-time catch-up and explicit dismissal passed. Delivery is in-app only. The disposable reminder task was dismissed.
- An uploaded transcript now produces a source-linked project checkpoint after exact review. Native resume preserves the source's unknown context, original clock, speaker and assignee distinctions. Task completion remains an owner confirmation, not an inference from transcript text.
- A real bounded background GitHub read added evidence subsequently visible in native Today with its original account and timestamps. Today also exposes recent indexed source updates separately from completed work. No universal source coverage is claimed.
- Chat and Today now share active-task, snooze and dormant-context rules. This fixes an observed omission of a legacy confirmed work obligation. Completed and dismissed tasks remain excluded. The installed retry found both exact records, corrected the earlier omission and preserved separate contexts, owners and freshness limitations.
- A disposable native shadow Mission survived relaunch while paused, resumed, executed both saved-record steps, displayed its output and was canceled. Isolated mid-step continuation is separately tested; no measured ROI is inferred.
- The official-export folder picker/import/dedup/status flow passed using one small synthetic export. Its folder grant was removed; imported history remains. An owner's real export folder is optional and no continuous account sync is claimed.
- Native capture now times out after 30 seconds without leaving an orphan selector. The current installed timeout/recovery path passed; successful physical region selection remains separate.

## Scoped browser boundary

The installed signed runtime driver reports Accessibility and Screen Recording enabled. This differs from builder computer access and does not prove successful capture or browser control.

Public cookie-free HTTPS read and download, wrong-account/consequential-action denial and grant revocation passed. Existing-profile Chrome requires a native exact-window confirmation that discloses a possible remote-debugging preference change. The new task-owned persistent MCP transport restricts typed tools, HTTPS origins, owner conversation, window and expiry. It exposes no generic clicks, arbitrary commands, submit action or global browser grant. Stop, expiry, failed startup and Full Quit request shutdown of only that task's driver; ordinary stage completion preserves the task scope. Stopping the driver does not restore Chrome's debugging preference.

The owner granted the separate setup consent. The actual installed bridge reached the official driver, which refused with `browser_wrong_target_refused`: the approved window had no exact New Tab button. A retry after renewed OS permission produced the same refusal. The driver returned an empty AX capture, so the cause is not proven. Both scoped connections stopped and test grants were revoked. Live existing-profile navigation/form preparation and the full native confirmation-dialog journey remain unaccepted. No supported selector override was available; no security bypass was attempted.

Window inventory now includes other macOS Spaces, uses the driver-resolved session identity, and filters exact grant resources before observing browser state. A registration lock and consent-generation barrier prevent a delayed setup from escaping Full Quit. Personal setup is the supported bounded path; broader account authority is not inferred.

## Remaining human and external checks

- Bring the approved personal Chrome window visibly onto the active desktop and retry its exact native browser selection. This is a diagnostic prerequisite, not a guaranteed fix for the official-driver refusal. Inspect Chrome remote debugging separately; shutdown receipts do not attest its preference state.
- Actual global Quick Entry shortcut with the main window hidden, real cross-application file drag, and successful selected-area capture remain unverified with the available app-targeted builder input. The Settings HUD launcher is already evidenced and is not equivalent to the keyboard shortcut.
- Fresh-room microphone, interruption, wake accuracy and aesthetic approval require owner judgment. Wake stays off unless deliberately enabled; no hidden listening was added.
- The private companion's responsive UI, authentication, expiry, CSRF and read/write boundaries have isolated evidence. Outside-home use requires an authorized private HTTPS transport and device pairing. No listener, public tunnel, remote-chat acceptance or messaging integration is claimed.
- Ad-hoc signing does not guarantee stable macOS permission continuity. Other-user distribution remains deferred.

## Validation and publication

The consolidated source passed 407 Python tests and 67 UI tests, including exact-body fact continuity, document lineage, reminder ownership, shared task-queue policy and scoped browser lifecycle/denials. Native build and affected installed checks are recorded separately in the private handoff. Private specifications, account/source records, screenshots and runtime state remain Git-excluded. Sanitized publication uses only the existing exact-repository guard on the existing feature branch.
