diff --git a/apps/desktop/src/app/chat/composer/hooks/use-composer-voice.ts b/apps/desktop/src/app/chat/composer/hooks/use-composer-voice.ts
index 449471dc07..c931b7d152 100644
--- a/apps/desktop/src/app/chat/composer/hooks/use-composer-voice.ts
+++ b/apps/desktop/src/app/chat/composer/hooks/use-composer-voice.ts
@@ -76 +76,2 @@ export function useComposerVoice({
-  /** Auto-speak selector: the latest unspoken reply only — a backlog collapses to the newest. */
+  /** Auto-speak selector: the latest unspoken final reply only.
+   * Interim tool narration stays visible but is never read as the answer. */
@@ -79 +80 @@ export function useComposerVoice({
-    const last = messages.findLast(m => m.role === 'assistant' && !m.hidden)
+    const last = messages.findLast(m => m.role === 'assistant' && !m.hidden && !m.interim)
diff --git a/apps/desktop/src/lib/chat-messages.test.ts b/apps/desktop/src/lib/chat-messages.test.ts
index 00b2bd7896..d2c83f410d 100644
--- a/apps/desktop/src/lib/chat-messages.test.ts
+++ b/apps/desktop/src/lib/chat-messages.test.ts
@@ -1076 +1076 @@ describe('collectUnspokenTurnSpeech', () => {
-  it('includes sealed interim narration AND the final answer of a tool-calling turn', () => {
+  it('replaces interim narration with one acknowledgement and speaks the complete final answer', () => {
@@ -1087 +1087 @@ describe('collectUnspokenTurnSpeech', () => {
-    expect(speech?.text).toBe('Let me check the clock.\n\nIt is 9 PM.')
+    expect(speech?.text).toBe("I'm checking that now.\n\nIt is 9 PM.")
@@ -1097,0 +1098 @@ describe('collectUnspokenTurnSpeech', () => {
+    expect(first?.text).toBe("I'm checking that now.")
@@ -1127 +1128 @@ describe('collectUnspokenTurnSpeech', () => {
-    expect(speech?.text).toBe('Narration done.')
+    expect(speech?.text).toBe("I'm checking that now.")
diff --git a/apps/desktop/src/lib/chat-messages.ts b/apps/desktop/src/lib/chat-messages.ts
index f50f4ed550..9ec955d60e 100644
--- a/apps/desktop/src/lib/chat-messages.ts
+++ b/apps/desktop/src/lib/chat-messages.ts
@@ -184 +184 @@ export interface UnspokenTurnSpeech {
- * Collect every unspoken assistant bubble after `lastSpokenId`, in order.
+ * Collect one short progress acknowledgement and the complete final answer.
@@ -186,7 +186,5 @@ export interface UnspokenTurnSpeech {
- * A turn with tool calls produces several assistant bubbles — narration
- * ("Let me check…") sealed as interims, then the final answer as a fresh
- * bubble. Voice conversation speaks a turn through ONE live session bound to
- * one response id, so it needs all of that text as a single growing string;
- * selecting only one bubble silently drops everything after it. The blank-line
- * join is a sentence boundary for the server's cutter, so a sealed bubble's
- * tail is flushed as soon as the next bubble starts.
+ * A tool-calling turn can emit several interim narration bubbles containing
+ * working notes. Speaking all of those is noisy and may expose reasoning. The
+ * first interim therefore becomes one neutral, immediate acknowledgement; the
+ * actual final assistant bubbles are then appended in full. Keeping the first
+ * interim id makes the aggregate append-only while the final answer streams.
@@ -201,0 +200 @@ export function collectUnspokenTurnSpeech(
+  let acknowledged = false
@@ -208,0 +208,9 @@ export function collectUnspokenTurnSpeech(
+    if (message.interim) {
+      id ??= message.id
+      if (!acknowledged) {
+        parts.push("I'm checking that now.")
+        acknowledged = true
+      }
+      continue
+    }
+
