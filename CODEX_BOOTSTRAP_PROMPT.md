# Codex Bootstrap — Use These Two Prompts in Order

1. Open this extracted repository as a trusted Codex project using **GPT-5.6 Sol**, **Medium reasoning**, and **Full Access**.
2. Trust the project-local hooks once so the deterministic safety checks load.
3. Paste the complete contents of `CODEX_PROMPT_01_CONTEXT_ACKNOWLEDGEMENT.md`. It must only read, verify, acknowledge, and stop—without changing anything.
4. Review the response. Then paste `CODEX_PROMPT_02_IMPLEMENTATION.md` in the **same Codex session** to begin the substantive build.

Do not combine the prompts. Prompt 1’s no-write boundary remains mandatory even though Full Access is enabled.
